<div class="container">
  <div class="row">
    <div class="col-md-3">

<h2>User info</h2>
<%= image_tag @user.get_profile_image(100,100) %>
<strong>name<%= @user.name %></strong>
<strong>introduction<%= @user.introduction %></strong>

<% if @user == current_user %>
  <i class="fa-solid fa-user-gear"><%= link_to edit_user_registration_path %></i>
<% end %>

    </div>
  </div>
</div>